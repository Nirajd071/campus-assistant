# NLP Engine dependencies
# NOTE: Heavy ML libraries (torch/transformers/sentencepiece) are intentionally
# NOT installed here. The NLP engine performs translation by calling the
# dedicated `nllb-translator` microservice over HTTP, and language detection is
# done with langdetect + script heuristics. Keeping this image light makes the
# Docker build fast and reliable.

# Language detection
langdetect>=1.0.9

# API Framework
fastapi>=0.85.0
uvicorn[standard]>=0.18.0
pydantic>=1.10.0
pydantic-settings>=2.0.0

# AI APIs
openai>=1.0.0
google-generativeai>=0.3.0

# Database & Caching
redis>=4.3.0

# HTTP & Utilities
httpx>=0.23.0
requests>=2.28.0
aiofiles>=0.8.0
aiohttp>=3.8.0

# Environment & Configuration
python-dotenv>=0.19.0
